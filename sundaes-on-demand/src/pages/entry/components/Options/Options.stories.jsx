import React from "react";
import Options from "./Options";

export default {
  title: "pages/Entry/Components/Options",
  component: Options,
};

export const DefaultState = () => <Options optionType="scoops" />;
